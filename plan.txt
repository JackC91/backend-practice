X npm init
X install dependencies
    X express
    X pg
    X dotenv (dev)
    X nodemon (dev)
X gitignore file for node modules
X app.js file at root
X set up inital router
X start script in package.json for app.js
- set up db
- db folder
    X index.js file with connection
    - add db credentials to .env file
    - scripts folder
- CRUD routes
    - POST
    - GET
    - PATCH/PUT
    - DELETE
- models folder with functions for each

- testing