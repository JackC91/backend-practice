X npm init
X install dependencies
    X express
    X pg
    X dotenv (dev)
    X nodemon (dev)
- gitignore file for node modules
- app.js file at root
- start script in package.json for app.js
- set up db
- db folder
    - index.js file with connection (.env for variables)
    - scripts folder
- CRUD routes
    - POST
    - GET
    - PATCH/PUT
    - DELETE
- models folder with functions for each

- testing